import React from "react";
import { ReactComponent as TransactionIcon } from "./svg/transactions-icon.svg";

interface Props {}
const Transaction: React.FC<Props> = () => {
  return <TransactionIcon />;
};

export default Transaction;
